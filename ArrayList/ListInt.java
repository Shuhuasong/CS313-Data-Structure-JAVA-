package ArrayList;

// A non-generic List interface
public interface ListInt {
    // Adds the specified element to the end of the list.
    void add(int e);

    // Returns the element at the specified index.
    int get(int index);

    int size();
}